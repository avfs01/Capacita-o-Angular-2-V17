// Importação nomeada
import { PI, soma } from './module';

// Importação padrão
import Usuario from './module';

// Importação de namespace
import * as Matematica from './module';

const Usual = PI;
console.log(soma);
console.log(Usual);