// Podemos definir if e else como estruturas condicionais; dado uma operação, se aquele resultado for verdadeiro (if), assuma os casos de if, caso contrario(else), assuma os casos de else.

// Exemplo:

if( 1 === 0){
 // verifique se a condição é verdadeira, se sim, entre nessa condicional
}
else if( 1 > 3){
// caso a ultima condição não tenha sido atendida, entre nessa condicional
}
else {
    // caso nenhuma condição for atendida, entre nessa.
}