// Tabuada
// Exercício prático durante a capacitação.

const multiplicador = 7; // declaração do multiplicador

for(let i=0;i<=10;i++){ // for loop para operar em todos os valores de 0 até n
    const calc = multiplicador * i; // Multiplicação do valor * índice.
    console.log(`${multiplicador} * ${i} = ${calc}`); // uso de crases para retornar funções dentro da string.
}